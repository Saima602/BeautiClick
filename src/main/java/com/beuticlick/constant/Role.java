package com.beuticlick.constant;

public enum Role {
    SALON_ADMIN,   // owns the salon — full access
    STAFF,         // salon employee — limited access
    CUSTOMER       // end customer — can only see their own data
}
